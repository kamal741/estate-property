# Copy to terraform.tfvars (gitignored) and set real values.
# Never commit terraform.tfvars — it is listed in the repository .gitignore.

project_id = "your-gcp-project-id"
region     = "us-central1"
