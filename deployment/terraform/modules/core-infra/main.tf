# Resources are split across sibling files for readability:
#   locals.tf, data.tf, apis.tf, network.tf, gke.tf, gcs.tf, sql.tf, redis.tf,
#   secrets.tf, artifact_registry.tf, jenkins.tf
# Provider constraints: terraform.tf. Variables: variables.tf. Outputs: outputs.tf.
