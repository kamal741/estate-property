apiVersion: v1
kind: Service
metadata:
  name: {{ include "estateflow-brokerage-agent-service.fullname" . }}
  labels:
    {{- include "estateflow-brokerage-agent-service.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "estateflow-brokerage-agent-service.selectorLabels" . | nindent 4 }}
